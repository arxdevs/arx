/target
**/*.rs.bk
Cargo.lock.bak

# Local config / data
/arx.db
/arx.db-*
/data
*.key

# Editor
.vscode/
.idea/
*.swp

# Coding agent local state (claude code, omc)
.claude/
.omc/

# OS
.DS_Store

# Web build artifacts
/web/node_modules
/web/dist
/web/.cache

# Embedded SPA build output. The directory is recreated by build.rs at compile
# time and (re)populated by `npm run build`, so nothing here is committed.
/crates/arx-server/web-dist/

# Local dev: do not commit this — auto-merged by `docker compose`
compose.override.yml
compose.override.*.yml
